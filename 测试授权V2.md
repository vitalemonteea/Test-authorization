# 测试设备授权平台 V2 - 需求描述文档

| 系统 | 版本 | 文档状态 |
|------|------|----------|
| 测试设备授权平台 | V2 | 编写中 |

---

## 5.0 申请授权模块

---

### 5.0.1 KA客户授权期限扩展

> ✅ 已在原型中实现 — 承接一期 4.2.5.6「二期扩展准备」

#### 1、初始状态

1. 统一授权日期栏新增日期限制标签，实时显示当前最大授权天数：
   - 普通客户（90天）：蓝色标签，文案"最长 90 天"
   - KA客户（180天）：橙色标签，文案"最长 180 天"
2. 日期输入框默认最大可选日期为当天起 90 天后，超出部分浏览器原生不可选
3. 客户信息展示区新增客户类别徽章，位于客户名称和客户ID右侧：
   - KA客户：橙色徽章，文案"KA客户"
   - 普通客户：绿色徽章，文案"普通客户"
4. 客户数据新增 `type` 字段（来源：客户数据主系统），取值为 `KA` 或 `normal`
5. 页面加载时默认日期限制为 90 天

#### 2、交互逻辑说明

1. 选择客户后动态调整日期限制：
   - 选择 KA 客户时：日期选择器最大可选日期扩展为 180 天，日期限制标签变为橙色"最长 180 天"，同步更新所有模块日期输入框的最大可选日期
   - 选择普通客户时：日期选择器最大可选日期恢复为 90 天，日期限制标签恢复为蓝色"最长 90 天"；若已选日期超出 90 天限制，自动清除已选日期并禁用应用按钮
2. 日期选择超出限制时的处理：
   - 输入框显示超范围样式（红框浅红底），"应用到所有模块"按钮禁用
   - 页面顶部弹出橙色警告提示，文案为"⚠ 授权日期不能超过今天起 N 天（具体截止日期）"
   - 警告提示 3 秒后自动消失
3. 客户类别徽章联动：选择客户后，根据客户类别更新徽章样式和文案（KA橙色 / 普通绿色）
4. 页面初始化时默认设置 90 天限制

#### 3、字段校验规则

1. 所有日期字段（含统一授权日期）不可超过当天起最大天数（90 或 180，取决于当前客户类别）
2. 选择超期日期时：输入框显示超范围样式，应用按钮禁用，弹出橙色警告提示
3. 客户类别切换时：重新校验已选日期是否在新限制范围内，超期则自动清除
4. 日期校验与当前客户类别实时联动

#### 4、提交逻辑处理

1. 客户类别标识随客户数据一同提交
2. 日期限制由前端控制，后端需进行二次校验以确保日期范围合规
3. 提交数据需包含：客户ID、客户名称、客户类别标识、各模块授权日期

#### 5、验收标准

1. 基础日期限制：
   - 页面加载后，日期选择器不可选择今天起 90 天之后的日期
   - 日期栏显示"最长 90 天"蓝色标签
   - 手动输入超出 90 天的日期时，输入框变红、按钮禁用、橙色警告弹出
2. KA客户特殊权限：
   - 选择KA客户后，日期选择器可选择今天起 180 天之内的日期
   - 日期限制标签变为"最长 180 天"橙色标签
   - 从KA客户切换为普通客户时，若已选日期超出 90 天限制，自动清除已选日期
3. 客户类别标识：
   - 选择客户后，客户信息栏显示客户类别徽章
   - KA客户显示橙色"KA客户"徽章
   - 普通客户显示绿色"普通客户"徽章

#### 6、与一期功能的承接关系

1. 本功能是一期 4.2.5.6「二期扩展准备（KA客户授权期限扩展）」的正式落地实现
2. 一期 4.2.1 中的 90 天硬编码限制已升级为动态天数机制（90 或 180）
3. 一期 4.2.5 中的客户类别徽章功能在本期与日期限制联动，形成完整的 KA 客户差异化授权流程
4. 当前原型中客户类别数据为模拟数据，生产环境需对接客户数据主系统的客户管理表单接口

---

### 5.0.2 授权场景与申请类型

> ✅ 已在原型中实现 — 双字段架构：授权场景标识业务类型，申请类型控制硬件/软件字段

#### 1、初始状态

1. 表单最前部新增两个字段（顺序：授权场景 → 申请类型）：
   - **授权场景**（id=authScene）：下拉选择，5个场景按3大类分组（optgroup）
     - 开通测试授权：开通测试授权(无授权/授权过期申请)（value=1）
     - 授权资源调整：授权资源调整(授权有效期内调整)（value=2）
     - 商务类：销售设备增开新模块测试授权（value=3）/ 销售授权扩容测试授权（value=5）/ 销售设备申请延长授权（value=6）
   - **申请类型**（id=planDevType）：下拉选择，硬件/软件二选一
     - value=1：一体机测试(部署在我司一体机，通过硬件设备开授权)
     - value=2：软件测试(部署在客户服务器，通过软件开授权)
2. 申请类型控制SN和借测字段的显隐：
   - 选value=1（硬件）→ 显示设备SN + 借测4字段
   - 选value=2（软件）→ 隐藏设备SN和借测字段
3. 授权场景控制业务相关区域：
   - value=2（授权资源调整）→ 显示已有授权展示区，隐藏硬件信息文件
   - value=3（销售增开）→ 显示设备SN + 已有授权展示区
   - value=5/6（商务待定）→ toast提示"正在建设中"

#### 2、交互逻辑说明

1. 授权场景切换：
   - 选择value=1（开通测试授权）：无特殊字段控制
   - 选择value=2（授权资源调整）：显示已有授权展示区（3种查询方式），隐藏硬件信息文件
   - 选择value=3（销售增开）：显示设备SN和已有授权展示区
   - 选择value=5/6：弹出"正在建设中"toast提示
2. 申请类型切换：
   - 选value=1（硬件）：显示设备SN字段 + 借测信息区域（4个字段）+ 设备ID + 硬件信息文件
   - 选value=2（软件）：隐藏设备SN和借测字段，显示设备ID + 硬件信息文件
3. **SN失焦自动检测**：
   - 申请类型=硬件时：SN失焦→关联借测数据
   - 授权场景=销售增开(value=3)时：SN失焦→查阅销售授权记录
4. 表单重置时，两个字段均恢复默认选项

#### 3、字段校验规则

1. 授权场景：必填，未选择时提示「请选择授权场景」
2. 申请类型：必填，未选择时提示「请选择申请类型」
3. 申请类型=硬件时：设备SN + 借测4字段必填
4. 设备ID：必填（两种申请类型都需要）
5. 授权场景=2时：必须查询到已有授权数据
6. 授权场景=3时：设备SN + 已有授权查询结果必填

#### 4、提交逻辑处理

1. 提交数据包含：授权场景值、申请类型值、以及对应字段数据
2. 授权场景value=5/6提交时拦截提示"该场景正在建设中"
3. 后端根据授权场景值路由至对应流程

#### 5、验收标准

1. 页面显示两个字段：授权场景（5选项3分组）→ 申请类型（2选项）
2. 申请类型选硬件→SN+借测显示，选软件→隐藏
3. 授权场景选"授权资源调整"→已有授权展示区显示
4. 授权场景选商务类→toast"正在建设中"
5. 两个字段都必选，提交校验正确
6. 表单重置后两个字段都恢复默认

---

### 5.0.3 授权资源调整（授权有效期内调整）

> ✅ 已在原型中实现 — 授权场景value=2，查询当前授权信息后执行调整操作

#### 1、初始状态

1. 授权场景选择「授权资源调整(授权有效期内调整)」后，表单显示「当前授权信息」查询区域：
   - 查询方式 Tab 切换（三选一）：设备ID查询 / SN查询 / 上传 .info 文件
   - 查询输入框 + 查询按钮（设备ID或SN模式）
   - 文件上传区域（上传 .info 文件模式）
   - 查询结果区域：设备ID、SN、已授权模块列表（模块名、授权状态、到期时间）
2. 设备ID字段同时显示，可用于填写新设备或作为补充
3. 硬件信息文件隐藏（由查询区域内的上传 .info 文件替代）
4. 业务规则：测试授权不区分扩容加点和增开模块，只要在产线范围内都可以测试，只关心授权时间

#### 2、交互逻辑说明

1. 授权场景切换为「授权资源调整」：
   - 显示已有授权展示区（当前授权信息查询区域）
   - 显示设备ID字段
   - 隐藏设备SN字段、借测信息区域
   - 隐藏硬件信息文件
2. 查询方式 Tab 切换：
   - 默认选中「设备ID查询」Tab
   - 切换到其他Tab时重置查询结果
   - 三个Tab互斥，同一时间只显示一个查询面板
3. 查询操作：
   - **设备ID查询**：输入设备ID后点击查询按钮，系统查询当前授权信息
   - **SN查询**：输入设备SN后点击查询按钮，系统查询当前授权信息
   - **上传 .info 文件**：上传设备.info文件后自动解析获取当前授权信息
4. 查询结果展示：
   - 查询成功：绿色卡片展示设备ID、SN、已授权模块列表（含模块名、授权状态徽章、到期时间）
   - 查询失败：橙色卡片提示"未查询到授权信息"，引导用户确认或换用其他查询方式
5. 切换到其他申请类型时：
   - 隐藏已有授权展示区，清空所有查询输入和结果

#### 3、字段校验规则

1. 授权场景：必填，未选择时提示「请选择授权场景」
2. 已有授权查询：必须完成查询且查询到有效数据，否则提交拦截
3. 设备SN和借测字段：由申请类型控制（申请类型=硬件时显示并校验）

#### 4、提交逻辑处理

1. 提交数据包含：授权场景（value=2）、查询到的设备ID、SN、已有授权模块列表
2. 后端根据申请类型 value=2 路由至测试授权变更流程
3. 调整类型（延长/增开/扩容）由用户在后续审批流程中确认，表单层面不区分

#### 5、验收标准

1. 授权场景选择后，已有授权展示区正确显示
2. 三种查询方式（设备ID/SN/上传.info文件）均可正常切换和使用
3. 输入有效设备ID（如`GW-2024-001`）查询后，正确展示已有授权模块列表
4. 输入有效SN（如`SN-2024001`）查询后，正确展示已有授权模块列表
5. 上传包含设备ID的.info文件后，自动解析并展示授权信息
6. 查询无结果时，显示橙色提示卡片
7. 未完成查询时提交，拦截并提示"请先查询设备当前授权信息"
8. 切换申请类型后，查询区域隐藏且所有输入和结果清空

---

### 5.0.4 销售设备增开新模块测试授权

> ✅ 已在原型中实现 — 授权场景value=3，通过SN查阅销售授权记录后增开模块

#### 1、初始状态

1. 授权场景选择「销售设备增开新模块测试授权」后，表单显示：
   - 设备SN：文本输入框，用于输入设备序列号查阅销售授权记录
   - 已有授权展示区：通过SN失焦自动查询，展示该设备当前的销售授权信息
   - 设备ID：文本输入框
2. 借测信息区域隐藏（无借测需求）
3. 硬件信息文件由产品线类型控制显隐

#### 2、交互逻辑说明

1. 授权场景切换为「销售设备增开新模块测试授权」：
   - 显示设备SN字段
   - 显示已有授权展示区
   - 显示设备ID字段
   - 隐藏借测信息区域
   - 硬件信息文件由产品线切换控制显隐
2. **SN失焦查阅销售授权记录**：用户输入设备SN并离开输入框后，系统自动查询该设备的销售授权记录。如查询到匹配数据，在已有授权展示区展示当前授权模块列表；如未查询到，显示"未查询到授权信息"提示
3. 从该类型切换到其他申请类型时：
   - 隐藏SN字段和已有授权展示区
   - 清空SN值和查询结果

#### 3、字段校验规则

1. 设备SN：必填，不可为空
2. 设备ID：必填，不可为空
3. 已有授权查询：必须通过SN查询到销售授权记录，否则提交拦截
4. 硬件信息文件（显示时）：必须上传 .info 格式文件
5. 借测字段：不参与校验（已隐藏）

#### 4、提交逻辑处理

1. 提交数据包含：授权场景（value=3）、设备SN、设备ID、查询到的销售授权记录
2. 后端根据授权场景 value=3 路由至销售设备增开模块流程
3. 审核人根据业务路由规则确定（与场景一不同）

#### 5、验收标准

1. 选择该申请类型后，SN字段和已有授权展示区正确显示，借测字段正确隐藏
2. 输入有效SN（如`SN-2024001`）失焦后，已有授权展示区正确显示销售授权模块列表
3. 输入无效SN失焦后，显示"未查询到授权信息"提示
4. 清空SN后失焦，查询结果清空
5. 所有必填字段未填写时，提交拦截并给出对应提示
6. 未通过SN查询到授权记录时提交，拦截并提示
7. 切换申请类型后，SN和查询结果清空

---

### 5.0.5 销售授权扩容测试授权

> 🔲 待定 — 存在授权覆盖问题，待业务方确认后补充（value=5）

#### 1、初始状态

（待补充：该场景的表单结构、授权覆盖的处理方式、与其他场景的差异）

#### 2、交互逻辑说明

（待补充）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充：选择该场景时暂拦截提示"该场景正在建设中"）

#### 5、验收标准

（待补充：当前验证标准为选择该场景后弹出"正在建设中"提示）

---

### 5.0.6 销售设备申请延长授权

> 🔲 待定 — 商务类场景，待业务方确认后补充（value=6）

#### 1、初始状态

（待补充：商务类延长授权的表单结构、与POC类延长授权(value=2)的差异）

#### 2、交互逻辑说明

（待补充）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充：选择该场景时暂拦截提示"该场景正在建设中"）

#### 5、验收标准

（待补充：当前验证标准为选择该场景后弹出"正在建设中"提示）

---

### 5.0.7 字段顺序与表单模式总览

> ✅ 已在原型中实现

#### 1、初始状态

1. 表单最前部为两个控制字段（顺序：授权场景 → 申请类型），之后为产品线等业务字段
2. **授权场景**（id=authScene）：5个选项，3大类分组（optgroup）：
   - 开通测试授权：开通测试授权(无授权/授权过期申请)（value=1）→ 5.0.2
   - 授权资源调整：授权资源调整(授权有效期内调整)（value=2）→ 5.0.3
   - 商务类：销售设备增开新模块(value=3)→5.0.4 / 销售授权扩容(value=5)→5.0.5 / 销售设备延长(value=6)→5.0.6
3. **申请类型**（id=planDevType）：2个选项：
   - 一体机测试(部署在我司一体机，通过硬件设备开授权)（value=1）
   - 软件测试(部署在客户服务器，通过软件开授权)（value=2）
4. 调整后表单字段顺序：
   1. 授权场景（分组下拉，3大类5选项）
   2. 申请类型（下拉，硬件/软件二选一）
   3. 产品线
   4. 版本
   5. 区域
   6. 办事处
   7. 接收邮箱
   8. 客户信息
   9. 设备SN（申请类型=硬件 或 授权场景=销售增开时显示）
   10. 借测单号（仅申请类型=硬件时显示）
   11. 借测使用人（仅申请类型=硬件时显示）
   12. 借测状态（仅申请类型=硬件时显示）
   13. 借测类型（仅申请类型=硬件时显示）
   14. 设备ID（始终显示）
   15. 已有授权展示区（授权场景=2或3时显示）
   16. 硬件信息文件（授权场景=2时隐藏，其余由产品线控制显隐）

#### 2、交互逻辑说明

1. 两个字段均为必填
2. 授权场景控制业务区域（已有授权展示区等）
3. 申请类型控制硬件相关字段（SN/借测）
4. 商务类授权场景选择后弹出"正在建设中"提示
5. 切换时清空被隐藏字段的值并重置查询状态

#### 3、字段校验规则

1. 授权场景必填，未选择时提示「请选择授权场景」
2. 申请类型必填，未选择时提示「请选择申请类型」

#### 4、提交逻辑处理

1. 两个字段值随表单一同提交
2. 后端根据授权场景值路由至对应流程
3. 商务类场景（value=5/6）提交时拦截提示"正在建设中"

#### 5、验收标准

1. 字段顺序：授权场景 → 申请类型 → 产品线 → 版本 → …
2. 授权场景下拉显示5个选项，按3大类分组
3. 申请类型下拉显示2个选项（硬件/软件）
4. 两个字段均必填，未选择时提交拦截并提示
5. 各场景字段显隐符合规则
6. 表单重置后两个字段都恢复默认状态

---

## 6.0 授权记录模块

---

### 6.0.1 授权记录申请变更（自动刷新新授权码）

> 🔲 待补充

#### 1、初始状态

（待补充：授权记录申请变更的入口、表单结构，以及申请通过后自动刷新新授权码的展示方式）

#### 2、交互逻辑说明

（待补充：含"申请通过后自动刷新新授权码"的子流程）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充）

#### 5、验收标准

（待补充）

---

### 6.0.2 调整单字段适配新申请类型格式

> 🔲 待补充

#### 1、初始状态

（待补充：授权记录列表中需适配新申请类型格式的字段及展示方式）

#### 2、交互逻辑说明

（待补充）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充）

#### 5、验收标准

（待补充）

---

### 6.0.3 授权申请支持复制

> 🔲 待补充

#### 1、初始状态

（待补充：授权记录列表中的复制操作入口、复制后的数据预填逻辑）

#### 2、交互逻辑说明

（待补充）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充）

#### 5、验收标准

（待补充）

---

### 6.0.4 授权记录支持一键续期

> 🔲 待补充

#### 1、初始状态

（待补充：一键续期操作入口、续期表单的预填逻辑）

#### 2、交互逻辑说明

（待补充）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充）

#### 5、验收标准

（待补充）

---

### 6.0.5 授权记录工单号传许可证平台

> 🔲 待补充

#### 1、初始状态

（待补充：工单号字段的展示方式、与许可证平台的数据传递机制）

#### 2、交互逻辑说明

（待补充）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充）

#### 5、验收标准

（待补充）

---

## 7.0 申请授权优化模块

---

### 7.0.1 版本选项优化

> 🔲 待补充

#### 1、初始状态

（待补充：版本选项的当前问题及优化方案）

#### 2、交互逻辑说明

（待补充）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充）

#### 5、验收标准

（待补充）

---

### 7.0.2 XDR硬件借测申请流程优化

> 🔲 待补充

#### 1、初始状态

（待补充：XDR硬件借测申请的当前流程及优化方案）

#### 2、交互逻辑说明

（待补充）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充）

#### 5、验收标准

（待补充）

