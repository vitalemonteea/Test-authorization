# 测试设备授权平台 V2 - 需求描述文档

| 系统 | 版本 | 文档状态 |
|------|------|----------|
| 测试设备授权平台 | V2 | 编写中 |

---

## 5.0 申请授权模块

---

### 5.0.1 KA客户授权期限扩展

> ✅ 已在原型中实现 — 承接一期 4.2.5.6「二期扩展准备」

#### 1、初始状态

1. 统一授权日期栏新增日期限制标签，实时显示当前最大授权天数：
   - 普通客户（90天）：蓝色标签，文案"最长 90 天"
   - KA客户（180天）：橙色标签，文案"最长 180 天"
2. 日期输入框默认最大可选日期为当天起 90 天后，超出部分浏览器原生不可选
3. 客户信息展示区新增客户类别徽章，位于客户名称和客户ID右侧：
   - KA客户：橙色徽章，文案"KA客户"
   - 普通客户：绿色徽章，文案"普通客户"
4. 客户数据新增 `type` 字段（来源：客户数据主系统），取值为 `KA` 或 `normal`
5. 页面加载时默认日期限制为 90 天

#### 2、交互逻辑说明

1. 选择客户后动态调整日期限制：
   - 选择 KA 客户时：日期选择器最大可选日期扩展为 180 天，日期限制标签变为橙色"最长 180 天"，同步更新所有模块日期输入框的最大可选日期
   - 选择普通客户时：日期选择器最大可选日期恢复为 90 天，日期限制标签恢复为蓝色"最长 90 天"；若已选日期超出 90 天限制，自动清除已选日期并禁用应用按钮
2. 日期选择超出限制时的处理：
   - 输入框显示超范围样式（红框浅红底），"应用到所有模块"按钮禁用
   - 页面顶部弹出橙色警告提示，文案为"⚠ 授权日期不能超过今天起 N 天（具体截止日期）"
   - 警告提示 3 秒后自动消失
3. 客户类别徽章联动：选择客户后，根据客户类别更新徽章样式和文案（KA橙色 / 普通绿色）
4. 页面初始化时默认设置 90 天限制

#### 3、字段校验规则

1. 所有日期字段（含统一授权日期）不可超过当天起最大天数（90 或 180，取决于当前客户类别）
2. 选择超期日期时：输入框显示超范围样式，应用按钮禁用，弹出橙色警告提示
3. 客户类别切换时：重新校验已选日期是否在新限制范围内，超期则自动清除
4. 日期校验与当前客户类别实时联动

#### 4、提交逻辑处理

1. 客户类别标识随客户数据一同提交
2. 日期限制由前端控制，后端需进行二次校验以确保日期范围合规
3. 提交数据需包含：客户ID、客户名称、客户类别标识、各模块授权日期

#### 5、验收标准

1. 基础日期限制：
   - 页面加载后，日期选择器不可选择今天起 90 天之后的日期
   - 日期栏显示"最长 90 天"蓝色标签
   - 手动输入超出 90 天的日期时，输入框变红、按钮禁用、橙色警告弹出
2. KA客户特殊权限：
   - 选择KA客户后，日期选择器可选择今天起 180 天之内的日期
   - 日期限制标签变为"最长 180 天"橙色标签
   - 从KA客户切换为普通客户时，若已选日期超出 90 天限制，自动清除已选日期
3. 客户类别标识：
   - 选择客户后，客户信息栏显示客户类别徽章
   - KA客户显示橙色"KA客户"徽章
   - 普通客户显示绿色"普通客户"徽章

#### 6、与一期功能的承接关系

1. 本功能是一期 4.2.5.6「二期扩展准备（KA客户授权期限扩展）」的正式落地实现
2. 一期 4.2.1 中的 90 天硬编码限制已升级为动态天数机制（90 或 180）
3. 一期 4.2.5 中的客户类别徽章功能在本期与日期限制联动，形成完整的 KA 客户差异化授权流程
4. 当前原型中客户类别数据为模拟数据，生产环境需对接客户数据主系统的客户管理表单接口

---

### 5.0.2 借测硬件和申请软件测试授权（首次申请）

> ✅ 已在原型中实现 — 硬件借测 + 软件授权一并申请

#### 1、初始状态

1. 申请类型选择「借测硬件和申请软件测试授权（首次申请）」后，表单显示以下专属字段：
   - 设备SN（设备序列号）：文本输入框，默认隐藏，选择该类型后显示
   - 借测信息区域（4个字段）：默认隐藏，选择该类型后显示
     - 借测单号：文本输入框
     - 借测使用人：文本输入框
     - 借测状态：下拉选择（办事处借测 / 渠道借测 / 自用测试）
     - 借测类型：下拉选择（POC测试 / 项目测试 / 演示测试 / 培训测试）
2. 其他公共字段同时显示：
   - 设备ID：文本输入框，始终显示
   - 硬件信息文件：由产品线类型控制显隐（仅 vkey 类型产品显示）
   - 标准字段：产品线、版本、区域、办事处、接收邮箱、客户信息
3. 页面加载时，若申请类型默认为 value=1 且为选中状态，SN和借测字段显示

#### 2、交互逻辑说明

1. 申请类型切换为「借测硬件和申请软件测试授权」：
   - 显示设备SN字段
   - 显示借测信息区域（借测单号、借测使用人、借测状态、借测类型）
   - 显示设备ID字段
   - 隐藏续期授权的历史借测数据区域
   - 硬件信息文件由产品线切换控制显隐
2. 从该类型切换到其他申请类型时：
   - 隐藏设备SN字段和借测信息区域
   - 清空SN和所有借测字段的值（下拉框恢复默认选项）
3. 产品线切换时：
   - 根据产品类型判断是否显示硬件信息文件
   - 不影响SN和借测字段的显示状态
4. **SN失焦自动检测借测数据**：在申请类型为「借测硬件和申请软件测试授权」时，用户输入设备SN并离开输入框后，系统自动发起借测数据检测。如检测到匹配的借测数据，自动填充借测单号、借测使用人、借测状态、借测类型；如未检测到匹配数据，清空所有借测字段并提示"未找到该SN对应的借测信息，请手动填写"。

#### 3、字段校验规则

1. 设备SN：必填，不可为空
2. 借测单号：必填，不可为空
3. 借测使用人：必填，不可为空
4. 借测状态：必填，必须选择一项
5. 借测类型：必填，必须选择一项
6. 设备ID：必填，不可为空
7. 硬件信息文件（显示时）：必须上传 .info 格式文件
8. 申请类型：必填，未选择时提示「请选择申请场景」
9. **SN关联借测数据**：系统不强制要求SN必须关联到借测数据（允许手动填写），但自动检测到匹配数据时会优先填充

#### 4、提交逻辑处理

1. 提交数据包含：申请类型（value=1）、设备SN、借测单号、借测使用人、借测状态、借测类型、设备ID、产品线、版本等基本信息
2. 硬件信息文件显示时，提交数据需包含文件校验结果
3. 后端根据申请类型 value=1 路由至硬件借测+软件授权流程处理
4. 借测数据需与借测系统对接校验（借测单号有效性、设备是否超期等）

#### 5、验收标准

1. 选择该申请类型后，设备SN字段和借测信息区域（4个字段）正确显示
2. 所有必填字段未填写时，提交拦截并给出对应提示
3. 切换到其他申请类型后，SN和借测字段隐藏且值被清空
4. 硬件信息文件根据产品线类型正确显隐
5. 表单提交数据包含完整的SN和借测信息
6. **SN自动关联检测**：
   - 输入有效SN（如`SN-2024001`）并在SN输入框失焦后，借测单号、借测使用人、借测状态、借测类型自动填充
   - 填充的数据与预设借测信息一致（单号BT20240615001、使用人张三、状态办事处借测、类型POC测试）
   - 输入无效SN（无匹配）失焦后，所有借测字段清空，页面弹出橙色提醒
   - 清空SN输入框后失焦，所有借测字段清空
   - 切换到非借测申请类型后，借测字段隐藏且值被清空

---

### 5.0.3 软件部署申请测试授权（首次申请）

> ✅ 已在原型中实现 — 纯软件部署，无需硬件借测

#### 1、初始状态

1. 申请类型选择「软件部署申请测试授权（首次申请）」后，表单**不显示**以下字段：
   - 设备SN（隐藏）
   - 借测信息区域（借测单号、借测使用人、借测状态、借测类型均隐藏）
2. 表单显示以下公共字段：
   - 设备ID：文本输入框，始终显示
   - 硬件信息文件：由产品线类型控制显隐（仅 vkey 类型产品显示）
   - 标准字段：产品线、版本、区域、办事处、接收邮箱、客户信息
3. 与 5.0.2 的核心差异：无需关联硬件借测信息，适用于纯软件部署场景

#### 2、交互逻辑说明

1. 申请类型切换为「软件部署申请测试授权」：
   - 隐藏设备SN字段
   - 隐藏借测信息区域
   - 显示设备ID字段
   - 隐藏续期授权的历史借测数据区域
   - 硬件信息文件由产品线切换控制显隐
2. 若从 value=1 切换到 value=2：
   - SN和借测字段隐藏并清空值
3. 产品线切换时：
   - 根据产品类型判断是否显示硬件信息文件
   - SN和借测字段始终保持隐藏

#### 3、字段校验规则

1. 设备ID：必填，不可为空
2. 硬件信息文件（显示时）：必须上传 .info 格式文件
3. 申请类型：必填，未选择时提示「请选择申请场景」
4. 设备SN和借测字段：不参与校验（已隐藏）

#### 4、提交逻辑处理

1. 提交数据包含：申请类型（value=2）、设备ID、产品线、版本等基本信息
2. 提交数据**不包含**：设备SN、借测单号、借测使用人、借测状态、借测类型
3. 硬件信息文件显示时，提交数据需包含文件校验结果
4. 后端根据申请类型 value=2 路由至软件部署授权流程处理

#### 5、验收标准

1. 选择该申请类型后，设备SN字段和借测信息区域正确隐藏
2. 设备ID字段正常显示且必填校验生效
3. 硬件信息文件根据产品线类型正确显隐
4. 从 value=1 切换到 value=2 后，SN和借测字段值被清空
5. 表单提交数据不包含SN和借测信息

---

### 5.0.4 续期授权场景融合

> ✅ 已在原型中实现 — 常规续期与超规续期统一入口

#### 1、初始状态

1. 申请类型选择「续期授权」后，表单字段动态调整：
   - 隐藏：硬件信息文件、设备ID
   - 显示：历史借测数据区域（紧跟申请类型字段，位于产品线上方）
2. 历史借测数据区域采用二选一 Tab 切换：
   - Tab 1：上传 .info 文件（复用硬件上传区样式，支持 .info 格式）
   - Tab 2：输入网关 ID（文本输入框 + 检测按钮）
3. 检测结果摘要卡片（默认隐藏，检测后显示）：
   - 标题栏：显示检测来源 + 续期类型状态徽章
   - 三栏网格布局：设备/网关 ID、历史授权到期日、续期类型
4. 超规续期额外字段（默认隐藏）：
   - 申请原因：多行文本框，自由填写
   - 附件上传：复用硬件上传区样式，提示“如有无法自助申请的审批附件，请在此上传”
5. 表单新增隐藏字段：renewalDeviceId、renewalType，用于提交检测数据

#### 2、交互逻辑说明

1. 申请类型切换为「续期授权」：
   - 隐藏 hwInfoSection、设备ID字段
   - 显示历史借测数据区域
2. 上传 .info 文件或输入网关 ID 并检测后：
   - 显示检测结果徽章（绿色「常规续期」或橙色「超规续期」）
   - 显示检测结果摘要卡片（含设备ID、到期日、续期类型）
   - 自动填充版本号字段（模拟查询历史借测数据）
   - 自动填充设备ID字段（隐藏）
   - 超规续期时额外显示申请原因和附件字段
3. 超规检测判定规则（前端模拟）：
   - 正则匹配文件名/网关ID中的 over/expired/超规/超期 关键词
   - 匹配到则为超规续期，否则为常规续期
4. Tab 切换或申请类型切换时，重置所有检测状态：
   - 清除文件、网关ID、徽章、摘要卡片、申请原因、附件
   - 版本号重置为默认值，设备ID重置为空
5. 产品线切换时，续期模式下跳过硬件信息文件的显隐控制

#### 3、字段校验规则

1. 历史借测数据必填（续期模式下）：需上传 .info 文件或输入网关 ID
2. 超规续期时，申请原因为必填字段
3. .info 文件格式校验：仅支持 .info 扩展名，不支持时提示「仅支持 .info 格式文件」

#### 4、提交逻辑处理

1. 提交数据包含：申请类型、renewalDeviceId、renewalType、版本、产品线等基本信息
2. 超规续期时额外提交：申请原因、附件文件
3. 后端根据 renewalType 区分常规/超规流程处理

#### 5、验收标准

1. 选择续期授权后，硬件信息文件和设备ID字段隐藏，历史借测数据区域出现在产品线上方
2. 上传 .info 文件后：
   - 显示检测结果徽章和摘要卡片
   - 版本和设备ID自动填充
   - 常规续期不显示额外字段
3. 输入网关 ID 并点击检测后：
   - 显示检测结果徽章和摘要卡片
   - 超规续期时显示申请原因和附件字段
4. Tab 切换后所有检测状态重置
5. 切换申请类型后，续期区域隐藏，所有字段恢复正常

---

### 5.0.5 超期扩容授权申请

> 🔲 待补充

#### 1、初始状态

（待补充：超期扩容授权申请的表单结构、新增字段、与现有申请类型的关系）

#### 2、交互逻辑说明

（待补充）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充）

#### 5、验收标准

（待补充）

---

### 5.0.6 销售设备申请延长授权

> 🔲 待补充

#### 1、初始状态

（待补充：销售设备延长授权申请的表单结构、字段定义）

#### 2、交互逻辑说明

（待补充）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充）

#### 5、验收标准

（待补充）

---

### 5.0.7 字段顺序变更

> ✅ 已在原型中实现

#### 1、初始状态

1. 申请类型字段从原「版本」下方移至表单**最前**（产品线之前），确保用户先选择申请场景再填写其他字段
2. 申请类型选项从原有 2 项拆分为 5 项，与 V2 功能模块对齐：
   - 借测硬件和申请软件测试授权（首次申请） → 对应 5.0.2
   - 软件部署申请测试授权（首次申请） → 对应 5.0.3
   - 续期授权（常规续期与超规续期统一入口） → 对应 5.0.4
   - 超期扩容授权申请 → 对应 5.0.5
   - 销售设备申请延长授权 → 对应 5.0.6
3. 调整后表单字段顺序：
   1. 申请类型（置于最前）
   2. 历史借测数据（续期授权时显示，位于产品线上方）
   3. 产品线
   4. 版本
   5. 区域
   6. 办事处
   7. 接收邮箱
   8. 客户信息
   9. 设备SN（仅 value=1 借测硬件+软件时显示）
   10. 借测单号（仅 value=1 时显示）
   11. 借测使用人（仅 value=1 时显示）
   12. 借测状态（仅 value=1 时显示）
   13. 借测类型（仅 value=1 时显示）
   14. 设备ID（续期授权时隐藏，由检测结果自动填充）
   15. 硬件信息文件（续期授权时隐藏，由历史借测数据区域替代）

#### 2、交互逻辑说明

1. 申请类型为必填字段，默认显示「请选择申请场景」
2. 选择不同申请类型后，根据对应场景动态显隐相关字段：
   - 选择「借测硬件和申请软件测试授权」时：显示设备SN + 借测4字段 + 设备ID + 硬件信息文件（产品线控制）
   - 选择「软件部署申请测试授权」时：隐藏设备SN和借测字段，显示设备ID + 硬件信息文件（产品线控制）
   - 选择「续期授权」时：隐藏设备SN、借测字段、硬件信息文件和设备ID，显示历史借测数据区域
   - 选择其他场景时：恢复默认字段显示逻辑
3. 切换申请类型时，清空被隐藏字段的值（SN、借测字段重置）
4. 产品线切换时，若当前为续期授权模式，跳过硬件信息文件的显隐控制

#### 3、字段校验规则

1. 申请类型必填，未选择时提示「请选择申请场景」

#### 4、提交逻辑处理

1. 申请类型值随表单一同提交，后端根据申请类型路由至对应流程处理

#### 5、验收标准

1. 字段顺序符合调整后的顺序：申请类型 → 产品线 → 版本 → 区域 → …
2. 申请类型下拉显示 5 个新场景选项
3. 申请类型为必填，未选择时提交拦截并提示
4. 选择「借测硬件+软件」时，设备SN和借测4字段正确显示
5. 选择「软件部署」时，设备SN和借测字段正确隐藏
6. 选择续期授权时，设备SN、借测字段、硬件信息文件和设备ID隐藏，历史借测数据区域出现
7. 切换申请类型时，被隐藏字段值被清空
8. 切换回其他申请类型时，所有字段恢复正常显示

---

## 6.0 授权记录模块

---

### 6.0.1 授权记录申请变更（自动刷新新授权码）

> 🔲 待补充

#### 1、初始状态

（待补充：授权记录申请变更的入口、表单结构，以及申请通过后自动刷新新授权码的展示方式）

#### 2、交互逻辑说明

（待补充：含"申请通过后自动刷新新授权码"的子流程）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充）

#### 5、验收标准

（待补充）

---

### 6.0.2 调整单字段适配新申请类型格式

> 🔲 待补充

#### 1、初始状态

（待补充：授权记录列表中需适配新申请类型格式的字段及展示方式）

#### 2、交互逻辑说明

（待补充）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充）

#### 5、验收标准

（待补充）

---

### 6.0.3 授权申请支持复制

> 🔲 待补充

#### 1、初始状态

（待补充：授权记录列表中的复制操作入口、复制后的数据预填逻辑）

#### 2、交互逻辑说明

（待补充）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充）

#### 5、验收标准

（待补充）

---

### 6.0.4 授权记录支持一键续期

> 🔲 待补充

#### 1、初始状态

（待补充：一键续期操作入口、续期表单的预填逻辑）

#### 2、交互逻辑说明

（待补充）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充）

#### 5、验收标准

（待补充）

---

### 6.0.5 授权记录工单号传许可证平台

> 🔲 待补充

#### 1、初始状态

（待补充：工单号字段的展示方式、与许可证平台的数据传递机制）

#### 2、交互逻辑说明

（待补充）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充）

#### 5、验收标准

（待补充）

---

## 7.0 申请授权优化模块

---

### 7.0.1 版本选项优化

> 🔲 待补充

#### 1、初始状态

（待补充：版本选项的当前问题及优化方案）

#### 2、交互逻辑说明

（待补充）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充）

#### 5、验收标准

（待补充）

---

### 7.0.2 XDR硬件借测申请流程优化

> 🔲 待补充

#### 1、初始状态

（待补充：XDR硬件借测申请的当前流程及优化方案）

#### 2、交互逻辑说明

（待补充）

#### 3、字段校验规则

（待补充）

#### 4、提交逻辑处理

（待补充）

#### 5、验收标准

（待补充）
