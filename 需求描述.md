# 测试设备授权平台 - 申请授权页面需求文档

---

## 4.2 UI优化&新增模块开关&导航栏

---

### 4.2.1 UI优化

#### 1、初始状态

1. 授权模块配置区每个模块以卡片形式展示，卡片底色 `#f8f9ff`，页面背景 `#fff`，形成视觉区分
2. 卡片容器（`module-item`）：
   - `background: #f8f9ff; border-radius: 12px; border: none; overflow: hidden`
   - `box-shadow: 0 1px 4px rgba(0,0,0,.06), 0 0 1px rgba(0,0,0,.04)`
   - `margin-bottom: 12px`
   - `max-width: 900px; margin-left: auto; margin-right: auto`（水平居中）
3. 卡片结构为：
   - 卡片头部（`module-head`）：`display: flex; align-items: center; padding: 14px 22px; gap: 12px`
     - 滑动开关（Toggle Switch） + 模块名称（`15px, font-weight: 600, color: #1a1a2e`） + 参数计数（`12px, color: #8b92a5`） + 状态标签（pill 样式） + 折叠箭头
   - 卡片内容区（`module-body`）：`border-top: 1px solid #eff4ff; padding: 14px 22px`
     - 双栏网格布局（`grid-template-columns: 1fr 1fr; gap: 8px 24px`），字段两两并排
     - 字段标签：`font-size: 14px; color: #424656; font-weight: 500; width: 120px`
     - 字段输入框：`max-width: 180px; height: 38px; border: 1px solid #e5eeff; border-radius: 8px`
   - 功能开关区（`module-features`）：位于内容区底部，`border-top: 1px solid #eff4ff; padding: 8px 0`
     - 以 "功能：" 为标题（`14px, color: #424656, font-weight: 500`），后接 pill 标签形式的功能开关
     - 开启状态：`background: #dae1ff; color: #0050cb`（无 border）
     - 关闭状态：`background: #e8ecf0; color: #8b92a5`（无 border）
     - hover 时微浮动效果：`transform: translateY(-1px); box-shadow: 0 2px 6px rgba(0,0,0,.06)`
4. 所有卡片直接平铺排列，无分组标题
5. 全局操作栏（`module-toolbar`）位于模块列表顶部：
   - `display: flex; align-items: center; padding: 10px 0; margin-bottom: 8px; border-bottom: 1px solid #eff4ff`
   - 标题 "授权模块配置"（`15px, font-weight: 600, color: #1a1a2e`）
   - "全部展开" 和 "全部折叠" 按钮（`height: 28px; padding: 0 12px; background: #eff4ff; color: #0050cb; border-radius: 8px; font-size: 12px`）
6. 统一授权日期栏（`unified-date-bar`）位于基本信息与模块配置区之间：
   - `display: flex; align-items: center; gap: 12px; background: #eff4ff; border-radius: 10px; padding: 14px 18px`
   - 标签 "统一授权日期"（`font-weight: 600; font-size: 14px; color: #333`）
   - 日期输入框 `width: 180px`，应用按钮 `height: 34px; background: #0050cb; color: #fff; border-radius: 8px`
   - 日期限制标签（`date-limit-hint`）：显示当前最大授权天数，默认显示"最长 90 天"（`font-size: 12px; color: #0050cb; background: #dce8ff; padding: 2px 10px; border-radius: 12px; white-space: nowrap; font-weight: 500`）
   - 日期输入框 `max` 属性默认设为当天起 90 天后的日期，超出部分不可选

#### 2、交互逻辑说明

1. 卡片折叠/展开
   - 点击卡片头部（开关除外）可切换内容区折叠/展开
   - 折叠箭头旋转：展开 `rotate(0)`，折叠 `rotate(-90deg)`，过渡 `transition: transform 0.2s`
   - 已禁用模块：内容区强制隐藏，点击头部不响应折叠操作
2. 全局操作栏
   - 点击 "全部展开"：所有已启用模块的卡片展开内容区，箭头旋转为展开态
   - 点击 "全部折叠"：所有已启用模块的卡片折叠内容区，箭头旋转为折叠态
3. 功能开关 Pill 交互
   - 点击功能 Pill 标签可切换开启/关闭状态
   - Pill 样式实时切换：开启（`background: #dae1ff; color: #0050cb`）与关闭（`background: #e8ecf0; color: #8b92a5`）
4. 统一授权日期操作
   - 选择日期后，"应用到所有模块" 按钮从禁用变为可用
   - 点击应用按钮：将统一日期填入所有已启用模块中所有日期类型字段
   - 操作后展示 Toast 提示（绿色提示条 `background: #4CAF50`，`position: fixed; top: 60px; right: 20px`）
5. 日期选择超出限制时的处理
   - 选择超过 90 天限制的日期时：日期输入框边框变红（`border-color: #c62828; background: #ffebee`），"应用到所有模块" 按钮禁用
   - 页面顶部弹出橙色警告提示（`background: #ff9800`），文案为"授权日期不能超过今天起 90 天（具体截止日期）"
   - 警告提示 3 秒后自动消失
6. 卡片 Hover 效果
   - 卡片 Hover：`box-shadow: 0 4px 12px rgba(0,0,0,.08)`
   - 卡片头部 Hover：`background: #f0f3ff`

#### 3、字段校验规则

1. 已禁用模块的字段不参与校验
2. 每个已启用模块的数值字段：需为正整数或正数，不超过字段规定的最大值
3. 每个已启用模块的日期字段：需为有效日期
4. 功能开关 Pill：无需校验，任何状态均为有效值
5. 校验展示方式：
   - 校验失败的字段旁显示红色提示文字（`font-size: 12px; color: #ba1a1a`）
   - 必填标记使用红色星号（`color: #ba1a1a`）
6. 日期范围校验：
   - 所有日期字段（含统一授权日期）不可超过当天起 90 天
   - 选择超期日期时，输入框红色高亮，应用按钮禁用，弹出橙色警告提示

#### 4、提交逻辑处理

1. 仅提交已启用模块的数据，已禁用模块不提交
2. 提交数据需包含各模块的字段值及功能开关状态

#### 5、验收标准

1. 卡片样式：所有模块卡片正确展示，底色 `#f8f9ff`、圆角 `12px`、阴影、间距与规范一致
2. 折叠/展开：
   - 点击卡片头部（开关除外）正确切换内容区显示/隐藏，箭头旋转方向正确
   - 已禁用模块点击头部不响应折叠
3. 全局操作栏：
   - "全部展开" 所有已启用模块内容区展开，箭头均为展开态
   - "全部折叠" 所有已启用模块内容区折叠，箭头均为折叠态
4. 功能 Pill 标签：点击后开启/关闭状态正确切换，样式实时更新
5. 统一授权日期：
   - 选择日期后应用按钮从禁用变为可用
   - 点击应用后所有已启用模块的日期字段被正确填充，Toast 提示正常显示
   - 选择 90 天范围内日期，应用按钮正常启用；选择超期日期，输入框变红、按钮禁用、橙色警告弹出
6. Hover 效果：卡片和头部 hover 时样式变化正确

---

### 4.2.2 模块开关

#### 1、初始状态

1. 新增滑动开关（Toggle Switch），位于模块名称左侧，带圆形滑块和过渡动画
2. 默认开启（蓝色），滑块位于右侧，状态标签显示"已启用"（蓝底白字 pill 样式）
3. 关闭时（灰色），滑块位于左侧，状态标签变为"已禁用"（灰底灰字 pill），卡片整体半透明，内容区折叠
4. 状态标签（`module-status`）：pill 样式
5. 新增模块时，模块名称需与当前产品线下的所有已有模块名称不重复

#### 2、交互逻辑说明

1. 模块开关联动
   - 点击滑动开关关闭模块时：
     - Toggle Switch 从蓝色变灰色
     - 状态标签从 "已启用" 变为 "已禁用"
     - 卡片整体变为半透明
     - 内容区自动折叠（`display: none`）
     - 需同步联动到授权平台关闭该模块的对应开关
   - 点击滑动开关开启模块时：
     - Toggle Switch 从灰色变蓝色
     - 状态标签恢复 "已启用"
     - 卡片透明度恢复
     - 内容区恢复上次折叠状态
     - 需同步联动到授权平台开启该模块的对应开关
   - 联动失败时：给出提示，开关恢复操作前的状态
2. 新增模块交互
   - 新增模块时，模块名称需与当前产品线下的所有已有模块名称不重复
   - 输入模块名称后，实时与当前已存在的模块名称列表进行比对
   - 若名称重复：阻止新增，弹出提示"模块名称已存在，请修改后重新添加"（红色提示文字）
   - 若名称唯一：正常创建模块卡片，插入到模块列表末尾，右侧模块导航同步新增该项

#### 3、字段校验规则

1. 已禁用模块的字段不参与校验（核心规则）
2. 模块名称：必填，不可为空；**需唯一**，不可与当前产品线下已有模块名称重复（不区分大小写）

#### 4、提交逻辑处理

1. 仅提交已启用模块的数据，已禁用模块不提交
2. 提交数据需包含各模块的开关状态（enabled/disabled）

#### 5、验收标准

1. Toggle Switch 视觉状态：
   - 开启时：蓝色，滑块在右侧，状态标签"已启用"（蓝底白字 pill）
   - 关闭时：灰色，滑块在左侧，状态标签"已禁用"（灰底灰字 pill）
2. 开关联动：
   - 关闭模块：卡片变半透明，内容区自动折叠，状态标签切换
   - 开启模块：卡片透明度恢复，内容区恢复上次状态，状态标签切换
   - 联动授权平台失败时：开关回滚到操作前状态，提示用户
3. 新增模块：
   - 输入已有模块名称时：阻止新增，弹出红色提示 "模块名称已存在"
   - 输入唯一名称时：正常创建卡片，右侧导航同步新增
   - 模块名称校验不区分大小写

---

### 4.2.3 模块导航

#### 1、初始状态

1. 右侧模块导航（`module-nav`）固定定位在页面右侧，白底圆角面板，可滚动，最大高度不超过视口
2. 导航标题："模块导航"
3. 导航项：悬停态浅蓝背景，激活态蓝底白字加粗
4. 列出当前产品线所有模块名称，第一个模块项为激活态
5. 内容区域右侧预留空间，避免与导航面板重叠

#### 2、交互逻辑说明

1. 点击导航项：页面平滑滚动到对应模块卡片位置（`scrollIntoView`）
2. 滚动页面：导航项自动高亮当前可视区域对应的模块
3. 新增模块时：右侧导航同步新增对应项
4. 产品线切换时：导航列表完全刷新，展示新产品线对应的模块列表

#### 3、字段校验规则

1. 无独立校验规则，导航项仅做展示和跳转

#### 4、提交逻辑处理

1. 导航栏不参与数据提交

#### 5、验收标准

1. 导航面板展示：固定定位在右侧，样式正确（白底、圆角、阴影），列出当前产品线所有模块
2. 点击导航项：页面平滑滚动到对应模块卡片位置
3. 滚动同步：滚动页面时导航项自动高亮当前可视区域对应的模块
4. 动态刷新：
   - 新增模块时导航同步新增对应项
   - 切换产品线时导航列表完全刷新
5. 内容区右侧预留 `200px` 空间，不与导航面板重叠

---

### 4.2.4 硬件信息文件上传

#### 1、初始状态

1. 在基本信息表单中，设备ID字段下方新增「硬件信息文件」上传字段
2. 该字段的显示条件依赖于产品授权类型判断：
   - **必填**：所有 vkey 类型的授权时，硬件信息文件为必填项
   - 数据源：【许可证授权】平台模版中 `"id_type": "clusterid"` 的产品
   - 当产品线匹配上述条件时显示该字段，否则自动隐藏
3. 上传区域（`hw-upload-area`）：flex 布局，所有元素始终同行排列（不换行）
   - 布局顺序：文件名容器（左）→ 上传按钮（右）→ 校验结果 pill
   - 原生文件输入框隐藏，使用自定义上传按钮触发
   - 文件名容器：输入框风格，hover 时边框加深，有文件时边框高亮为主题色
     - 文件名超长时截断（省略号），未选择时显示灰色占位文字"未选择文件（支持 .info 格式）"
     - 文件元信息：展示文件大小等辅助信息
     - 删除按钮：圆形 × 按钮，仅在有文件时显示，默认隐藏
   - 上传按钮：主题色按钮，带上传图标 ⇧，hover 时有阴影效果
   - 校验结果标签：与按钮、文件名同行排列，空状态时自动隐藏
   - 提示文字集成在文件名占位状态中，不再单独显示
4. 左侧标签与上传区元素垂直居中对齐
5. 页面加载时根据默认产品线自动判断是否显示该字段
6. 校验结果 pill 标签：格式校验不通过时显示浅红底深红字，格式正确时不显示任何提示

#### 2、交互逻辑说明

1. 产品线联动显示/隐藏
   - 切换到 vkey 类型授权产品：显示上传字段
   - 切换到非 vkey 类型产品：隐藏上传字段，同时清空已选文件和校验结果
2. 文件选择后自动触发校验
   - 格式校验：文件名不以 `.info` 结尾时，显示 pill 标签"仅支持 .info 格式文件"（浅红底深红字）
   - 格式正确后静默通过，不显示任何提示（避免与后端校验结果冲突）
3. 删除文件
   - 点击文件名容器内的 × 按钮：清空文件输入、重置文件名占位文字、清除文件大小和校验结果
4. 清空文件时：校验结果区域恢复为空

#### 3、字段校验规则

1. 仅当该字段显示（即当前产品为 vkey 类型授权，`id_type: clusterid`）时才参与校验
2. **必填**：字段显示时，必须上传 `.info` 格式文件，未上传时提示 "请上传硬件信息文件"（浅红底 pill 标签）
3. 文件格式：必须为 `.info` 后缀（前端限制 + JS 兜底校验）
4. 文件内容合规性：上传后自动检查硬件信息是否符合授权标准（由后端处理，前端静默通过）

#### 4、提交逻辑处理

1. 当该字段显示时，提交数据需包含硬件信息文件的校验结果
2. 校验不通过时，表单仍可提交但需提示用户确认

#### 5、验收标准

1. 字段显示/隐藏：
   - 选择 vkey 类型授权产品（`id_type: clusterid`）时，字段正确显示
   - 选择非 vkey 类型产品时，字段隐藏，已上传文件自动清空
2. 文件上传：
   - 点击上传按钮正确触发文件选择器，仅接受 `.info` 格式
   - 上传后文件名和文件大小正确显示在文件名容器内
   - 格式正确时前端静默通过，无额外提示
   - 格式错误时显示红色 pill 提示 "仅支持 .info 格式文件"
3. 删除文件：点击 × 按钮后文件清空，文件名恢复占位文字，文件大小和校验结果清除
4. 必填校验：字段显示时未上传文件，提交时提示 "请上传硬件信息文件"
5. 布局稳定性：无论有无文件、有无校验结果，所有元素始终保持同行，不换行、不跳动

---

### 4.2.5 客户信息字段

#### 1、初始状态

1. 在基本信息表单中，联系人字段下方新增「客户信息」搜索选择字段
2. 搜索区域：最大宽度 300px，搜索输入框 placeholder "请输入客户名称或ID搜索"
3. 搜索结果下拉：浮层展示，最多显示 10 条，可滚动
   - 每项显示客户名称和客户ID
4. 选中客户信息展示区：默认隐藏，选中客户后显示
   - 展示内容：客户名称 + 客户ID + 客户类别标识徽章
5. 客户类别标识徽章：
   - KA客户：橙色徽章，文案"KA客户"
   - 普通客户：绿色徽章，文案"普通客户"
6. 客户数据来源：客户数据主系统的客户管理表单，包含客户名称、客户ID、客户类别标识（`type` 字段，取值为 `KA` 或 `normal`）

#### 2、交互逻辑说明

1. 搜索输入
   - 输入客户名称或ID时，实时模糊匹配，下拉显示最多 10 条结果
   - 无匹配时显示"无匹配结果"灰色提示
   - 清空输入时关闭下拉
2. 选择客户
   - 点击下拉项后：搜索框填入客户名称，下方展示客户详细信息（名称、ID、类别徽章）
   - KA客户名称旁在下拉中标注 `[KA]`（橙色）
   - 选中客户后根据客户类别更新类别徽章样式和文案
3. 点击搜索区域外部时关闭下拉

#### 3、字段校验规则

1. 客户信息：字段显示时为必填，必须选择一个客户
2. 选中客户数据需包含客户ID和客户类别标识

#### 4、提交逻辑处理

1. 提交数据需包含选中的客户ID和客户名称
2. 客户类别标识随客户数据一同提交

#### 5、验收标准

1. 客户搜索：输入名称或ID后下拉实时显示匹配结果，无匹配时显示灰色提示
2. 选择客户：选中后正确展示客户名称、ID和类别徽章
3. 类别徽章：KA客户显示橙色"KA客户"徽章，普通客户显示绿色"普通客户"徽章
4. 下拉列表：KA客户名称旁显示橙色 `[KA]` 标识
5. 清空输入时下拉关闭，展示区隐藏

#### 6、二期扩展准备（KA客户授权期限扩展）

> ✅ **已在 V2 中实现**，详见《测试授权V2.md》`5.0.1 KA客户授权期限扩展`。

以下功能已在二期原型中落地：

1. 当选择 KA 客户（`type: 'KA'`）时，统一授权日期选择器的最大天数从 90 天动态调整为 180 天
2. 日期限制标签从"最长 90 天"（蓝色）变为"最长 180 天"（橙色）
3. 从 KA 客户切换为普通客户时，若已选日期超出 90 天限制，自动清除已选日期
4. 客户类别标识来源需与客户数据主系统的客户管理表单保持严格同步（当前原型为模拟数据，生产环境需对接接口）
