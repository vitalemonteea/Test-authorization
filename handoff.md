# 解决方案型测试授权 Demo - Handoff

## 1. 工作区信息

- Worktree：`D:\原型\测试授权-解决方案型`
- 分支：`codex/solution-authorization-demo`
- 基线提交：`e6132f2895dd83b66f58de73e95f7af70245fe1d`
- 基线来源：当前测试授权 V2 Demo 的已提交页面和能力
- 需求归属：这是独立的“解决方案型测试授权”需求，不属于 V2；不要回到 `D:\原型\测试授权-v2` 修改。

## 2. 接手后的第一步

按顺序阅读：

1. `AGENTS.md`
2. `docs/解决方案型测试授权申请概要方案.md`，这是当前业务规则的唯一依据
3. `测试设备授权平台V2.html`，作为页面、组件和交互能力基线
4. `prototype-授权记录列表.html`，了解解决方案授权记录的预留结构
5. `tests/record-workflows.test.cjs` 和 `authorization-application-rules.js`

不要沿用旧文档或旧讨论中的“资产审批后发消息给 W3、W3 自动生成授权记录”方案，该方案已经废弃。

## 3. 当前任务目标

在本 worktree 内，基于现有 V2 Demo 改造出解决方案型测试授权 Demo。页面需要覆盖两个不同的业务对象：

1. **解决方案配置页**：W3 内部维护解决方案与产品线关系。
2. **解决方案授权申请页**：业务人员在 W3 关联借测单或创建纯软件申请，补充授权参数并提交整单审批。

优先完成配置页和申请主流程，再补齐审批预览、失败状态和授权记录联动。不要实现新资产系统页面，只在 W3 中使用 Mock 数据模拟配置查询和借测单查询。

## 4. 已确认业务规则

### 4.1 配置页

- 配置数据归 W3。
- 只维护“解决方案与产品线”的关系。
- 不维护版本、设备、授权模块、容量、有效期、硬件规则或软件规则。
- 新资产系统通过接口读取启用配置，不打开、不嵌入 W3 配置页。
- 配置停用不影响历史借测单和历史授权记录。

### 4.2 关联借测单的申请

- 新资产系统借测审批和 W3 授权审批完全独立。
- 新资产系统审批通过后不发送消息给 W3，不自动创建 W3 申请或授权记录。
- 业务人员主动进入 W3 发起解决方案型申请。
- W3 只能关联“审批通过且未归还、未撤销”的借测单。
- W3 可通过借测单获取全部申请信息，包括借测单、申请人、客户、方案、产品线、设备、期限和审批信息。
- 方案和产品线以借测单已审批内容为准，只读展示，不允许增加、删除或替换。
- 用户仅补充实际授权所需的版本、设备 ID、授权模块、规格参数和有效期等信息。
- 提交后必须重新走 W3 审批，资产审批不能替代 W3 审批。

### 4.3 纯软件解决方案

- 纯软件方案可直接在 W3 创建，不要求关联借测单。
- 选择 W3 已启用的解决方案后自动带出产品线，产品线仍不可调整。
- 申请必须走 W3 审批，驳回后允许修改重提。
- 不创建或修改新资产系统借测单，不向新资产系统发送业务数据。

### 4.4 审批、授权和记录

- 一套解决方案是一张申请单、一整单审批，不按产品线拆分审批单。
- 任一审批节点驳回则整单驳回。
- 全部产品授权成功才算整单成功。
- 任一产品授权失败时，回滚本次已成功产品，不允许部分成功。
- 一套解决方案只生成一条 `SOL-...` 授权记录。
- 授权记录主列表展示一行，展开后查看各产品的版本、设备、有效期和授权码。
- 现有 V2 Demo 已预留解决方案记录和展开行，优先复用，不要重新发明另一套记录结构。

## 5. 页面范围建议

### 5.1 解决方案配置页

至少包括：

- 方案列表：方案编码、方案名称、关联产品线、状态、更新时间、操作。
- 搜索与状态筛选。
- 新增/编辑方案：填写编码、名称，多选产品线。
- 启用/停用。
- 查看被资产系统读取的数据效果可使用轻量预览，不需要制作资产系统页面。

### 5.2 解决方案授权申请页

至少包括：

- 申请来源选择或明确入口：关联借测单 / 纯软件本地申请。
- 借测单搜索与状态校验。
- 借测信息带出区。
- 方案和产品线只读展示区。
- 按产品线填写授权参数的配置区。
- 整单审批预览和提交结果。
- 产品线多时必须便于扫描、定位和校验，避免把所有字段堆成一个长表单。

### 5.3 授权记录

- 复用 `测试设备授权平台V2.html` 中 `.solution-parent` + `.solution-detail-row` 结构。
- 保留类型筛选“解决方案”。
- 新申请成功后应能在记录区看到一条解决方案记录并展开查看产品明细。

## 6. 明确不做

- 不开发新资产系统页面。
- 不实现资产审批后消息通知 W3。
- 不自动创建 W3 申请、授权任务或授权记录。
- 不允许在 W3 调整借测单带出的方案和产品线。
- 不在配置页维护具体授权参数或授权规则。
- 不按产品线拆分审批单。
- 不允许部分产品授权成功。
- 不修改 `D:\原型\测试授权-v2` worktree 中的文件。

## 7. 待确认但不阻塞页面探索的内容

- W3 整单审批的节点数量、审批人和具体规则。
- 配置页维护角色、权限范围和审计要求。
- 同一借测单是否允许创建多张 W3 申请。
- 配置停用后，已审批但尚未在 W3 申请的借测单是否仍可继续。
- 借测单延期、归还后，W3 如何获知并处理已有授权。
- 下游许可证平台撤销或作废接口是否能支撑整单回滚。

这些内容在 Demo 中可使用清晰的 Mock 状态表达，但不要擅自固化成正式业务规则。

## 8. 关键文件与现有能力

- `测试设备授权平台V2.html`：主 Demo，包含申请页、审批/记录相关交互。
- `authorization-application-rules.js`：授权申请规则模型。
- `prototype-授权记录列表.html`：授权记录独立原型。
- `tests/record-workflows.test.cjs`：授权记录和操作流程测试。
- `测试授权V2.md`：仅用于理解基线能力，不作为本需求业务规则来源。
- `docs/解决方案型测试授权申请概要方案.md`：本需求业务规则来源。

现有解决方案记录定位参考：

- `测试设备授权平台V2.html` 中搜索 `solution-parent`、`solution-detail-row`、`SOL-202607`。
- `tests/record-workflows.test.cjs` 中已有解决方案 Mock 记录断言。

## 9. 实施约束

- 延续现有 HTML/CSS/JS 技术栈和设计语言，不引入新前端框架。
- 优先复用已有表单、选择器、抽屉、状态标签、审批预览和记录组件。
- 功能模块不提前写死未确认的审批人或接口字段。
- 保持桌面端和移动端可用，避免产品线多时横向溢出和内容遮挡。
- 改动前先检查 `git status`，不要覆盖用户已有修改。
- 手动修改文件使用 `apply_patch`。

## 10. 验证与交付

完成修改后至少执行：

```powershell
npm test
git diff --check
```

页面修改需要在浏览器中验证：

- 配置新增、编辑、启停和筛选。
- 借测单有效、未审批、已归还、已撤销和查询失败状态。
- 产品线只读且无法绕过修改。
- 整单提交、驳回和授权失败回滚演示。
- 一条解决方案记录的生成与展开。
- 桌面端和移动端无重叠、溢出或不可操作控件。

若用户要求打包，必须按 `AGENTS.md` 使用 `web-mock-build-deploy`，打包入口重命名为 `index.html` 并包含全部静态资源。
